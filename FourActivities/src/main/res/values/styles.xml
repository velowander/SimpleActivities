<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.NoTitleBar.Fullscreen">
        <!-- Customize your theme here. -->
    </style>

    <style name="Jabberwocky" parent="@android:style/Theme.NoTitleBar.Fullscreen">
        <!-- Customize your theme here. -->
        <item name="android:background">#900000</item>
        <item name="android:textColor">#000000</item>
    </style>

    <style name="NASA" parent="@android:style/Theme.NoTitleBar.Fullscreen">
        <!-- Customize your theme here. -->
        <item name="android:background">#0052A3</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>


</resources>
